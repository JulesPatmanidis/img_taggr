{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the img-taggr window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
