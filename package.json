{
  "name": "img-taggr",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:wasm": "./build-wasm.sh",
    "web": "./build-wasm.sh && ./dev-server.py"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
