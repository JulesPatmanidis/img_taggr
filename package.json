{
  "name": "img-taggr",
  "version": "0.1.0",
  "description": "Edit photo time, date and location metadata through a map and a timeline",
  "license": "MIT",
  "repository": "github:JulesPatmanidis/img_taggr",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "test:rust": "cargo test --manifest-path engine/Cargo.toml && cargo test --manifest-path desktop/Cargo.toml",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:wasm": "./build-wasm.sh",
    "web": "./build-wasm.sh && ./dev-server.py"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
